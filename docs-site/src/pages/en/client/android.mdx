# Android client

The Android APK embeds the KnotRoute core and does not use `VpnService`/TUN. Add a network using a `knotroute://join` deep link/QR or manually enter the network ID and Beacon URLs. Install the local CA through Android's system certificate installer for HTTPS `.knot` browsing.
