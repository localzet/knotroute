# Beacon and relays

Beacon is soft-state peer discovery, not a central service registry. Publish the Beacon HTTP endpoint through HTTPS and expose the raw KnotRoute relay transport separately on TCP/7447.
